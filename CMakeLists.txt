cmake_minimum_required(VERSION 3.10)
set (CMAKE_CXX_STANDARD 11)
project(cml)
add_compile_options(-O3 -Wall -Wpedantic)
file(GLOB_RECURSE SOURCES src/*.c)
add_executable(cml ${SOURCES})
target_include_directories(cml PRIVATE src)
